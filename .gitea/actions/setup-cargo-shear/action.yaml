name: Setup cargo-shear
description: Install a pinned cargo-shear

inputs:
  version:
    required: false
    default: "1.13.3"

runs:
  using: composite
  steps:
    - shell: bash
      run: cargo install cargo-shear --locked --version "${{ inputs.version }}"
