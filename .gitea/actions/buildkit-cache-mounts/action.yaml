name: BuildKit cache mounts
description: Persist Dockerfile cache mounts across CI runs

inputs:
  image:
    required: false
    default: https://github.com/reproducible-containers/buildkit-cache-dance@2925f5cfd92896e0f5a155899ff2ed2922b575c6 # v3.4.0
  name:
    required: true
  builder:
    required: true
  dockerfile:
    required: false
    default: "./Dockerfile"
  cache-dir:
    required: false
    default: .buildkit-cache-mounts
  key:
    required: false
    default: ""
  restore-keys:
    required: false
    default: ""
  cache-map:
    required: false
    default: |
      {
        "rustup": { "target": "/usr/local/rustup", "id": "rustup" },
        "cargo-registry": { "target": "/usr/local/cargo/registry", "id": "cargo-registry" },
        "cargo-git": { "target": "/usr/local/cargo/git", "id": "cargo-git" }
      }
  targets:
    description: platform=rust-target lines for multi-arch builds
    required: true

runs:
  using: composite
  steps:
    - id: map
      shell: bash
      env:
        TARGETS: ${{ inputs.targets }}
        CACHE_MAP: ${{ inputs.cache-map }}
        CACHE_DIR: ${{ inputs.cache-dir }}
      run: |
        set -euo pipefail
        bash "${{ gitea.workspace }}/.gitea/scripts/docker-targets.sh"
        {
          echo "targets_fp=$(printf '%s' "${TARGETS}" | sha256sum | cut -c1-16)"
        } >> "${GITEA_OUTPUT}"

    - id: cache
      uses: ./.gitea/actions/cache
      with:
        path: ${{ inputs.cache-dir }}
        prefix: ${{ inputs.name }}
        key: ${{ inputs.key || format('cargo-mounts-{0}-{1}-{2}', hashFiles('**/Cargo.lock', 'rust-toolchain.toml'), hashFiles(inputs.dockerfile), steps.map.outputs.targets_fp) }}
        restore-keys: ${{ inputs.restore-keys || format('cargo-mounts-{0}-', hashFiles('**/Cargo.lock', 'rust-toolchain.toml')) }}

    - uses: ${{ inputs.image }}
      with:
        builder: ${{ inputs.builder }}
        cache-map: ${{ steps.map.outputs.cache_map }}
        dockerfile: ${{ inputs.dockerfile }}
        skip-extraction: ${{ steps.cache.outputs.cache-hit }}
